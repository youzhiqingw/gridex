<Page
    x:Class="Gridex.HomePage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    Background="Transparent">

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="340" />
            <ColumnDefinition Width="Auto" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <!-- Left Sidebar -->
        <Grid Grid.Column="0" Padding="24">
            <Grid.RowDefinitions>
                <RowDefinition Height="*" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>

            <!-- Branding (centered in available space) -->
            <StackPanel Grid.Row="0"
                        VerticalAlignment="Center"
                        HorizontalAlignment="Center"
                        Spacing="8">
                <Image Source="ms-appx:///Assets/gridex-icon-source.png"
                       Width="80" Height="80"
                       HorizontalAlignment="Center" />
                <TextBlock Text="Gridex"
                           FontSize="36"
                           FontWeight="Light"
                           HorizontalAlignment="Center"
                           Foreground="{ThemeResource TextFillColorPrimaryBrush}" />
                <TextBlock Text="AI 原生数据库 IDE"
                           FontSize="14"
                           HorizontalAlignment="Center"
                           Foreground="{ThemeResource TextFillColorSecondaryBrush}" />
            </StackPanel>

            <!-- Bottom Actions (pinned to bottom like macOS) -->
            <StackPanel Grid.Row="1" Spacing="4">
                <Button HorizontalAlignment="Stretch"
                        HorizontalContentAlignment="Left"
                        Padding="8,8"
                        Background="Transparent"
                        BorderThickness="0"
                        Click="NewConnection_Click">
                    <StackPanel Orientation="Horizontal" Spacing="10">
                        <FontIcon Glyph="&#xE710;" FontSize="14"
                                  Foreground="{ThemeResource TextFillColorSecondaryBrush}" />
                        <TextBlock Text="新建连接"
                                   Foreground="{ThemeResource TextFillColorSecondaryBrush}" />
                    </StackPanel>
                </Button>
                <Button HorizontalAlignment="Stretch"
                        HorizontalContentAlignment="Left"
                        Padding="8,8"
                        Background="Transparent"
                        BorderThickness="0"
                        Click="NewGroup_Click">
                    <StackPanel Orientation="Horizontal" Spacing="10">
                        <FontIcon Glyph="&#xE8B7;" FontSize="14"
                                  Foreground="{ThemeResource TextFillColorSecondaryBrush}" />
                        <TextBlock Text="新建分组"
                                   Foreground="{ThemeResource TextFillColorSecondaryBrush}" />
                    </StackPanel>
                </Button>
                <Button HorizontalAlignment="Stretch"
                        HorizontalContentAlignment="Left"
                        Padding="8,8"
                        Background="Transparent"
                        BorderThickness="0"
                        Click="Settings_Click">
                    <StackPanel Orientation="Horizontal" Spacing="10">
                        <FontIcon Glyph="&#xE713;" FontSize="14"
                                  Foreground="{ThemeResource TextFillColorSecondaryBrush}" />
                        <TextBlock Text="设置"
                                   Foreground="{ThemeResource TextFillColorSecondaryBrush}" />
                    </StackPanel>
                </Button>
            </StackPanel>
        </Grid>

        <!-- Divider -->
        <Border Grid.Column="1"
                Width="1"
                Background="{ThemeResource DividerStrokeColorDefaultBrush}" />

        <!-- Right Panel — Connections -->
        <Grid Grid.Column="2" Padding="24,24,24,24">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>

            <!-- Search Row: SearchBox + Action Buttons (like macOS) -->
            <Grid Grid.Row="0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>

                <AutoSuggestBox x:Name="SearchBox"
                                Grid.Column="0"
                                PlaceholderText="搜索连接..."
                                QueryIcon="Find"
                                VerticalAlignment="Center"
                                TextChanged="SearchBox_TextChanged" />

                <!-- + New Connection Button -->
                <Button Grid.Column="1"
                        Margin="8,0,0,0"
                        Width="36" Height="36"
                        Padding="0"
                        ToolTipService.ToolTip="新建连接"
                        Click="NewConnection_Click">
                    <FontIcon Glyph="&#xE710;" FontSize="14" />
                </Button>

                <!-- Group Button -->
                <Button Grid.Column="2"
                        Margin="4,0,0,0"
                        Width="36" Height="36"
                        Padding="0"
                        ToolTipService.ToolTip="新建分组"
                        Click="NewGroup_Click">
                    <FontIcon Glyph="&#xE8B7;" FontSize="14" />
                </Button>
            </Grid>

            <!-- Connection List -->
            <ListView x:Name="ConnectionsListView"
                      Grid.Row="1"
                      Margin="0,16,0,0"
                      IsItemClickEnabled="True"
                      ItemClick="ConnectionItem_Click"
                      SelectionMode="Single">
                <ListView.ItemContainerStyle>
                    <Style TargetType="ListViewItem">
                        <Setter Property="Padding" Value="0" />
                        <Setter Property="Margin" Value="0,0,0,4" />
                        <Setter Property="HorizontalContentAlignment" Value="Stretch" />
                    </Style>
                </ListView.ItemContainerStyle>
            </ListView>

            <!-- Empty State -->
            <StackPanel x:Name="EmptyState"
                        Grid.Row="1"
                        VerticalAlignment="Center"
                        HorizontalAlignment="Center"
                        Spacing="8"
                        Visibility="Collapsed">
                <FontIcon Glyph="&#xE774;" FontSize="48"
                          Foreground="{ThemeResource TextFillColorTertiaryBrush}"
                          HorizontalAlignment="Center" />
                <TextBlock Text="还没有连接"
                           FontSize="16"
                           HorizontalAlignment="Center"
                           Foreground="{ThemeResource TextFillColorTertiaryBrush}" />
                <TextBlock Text="先创建第一个数据库连接开始使用"
                           FontSize="12"
                           HorizontalAlignment="Center"
                           Foreground="{ThemeResource TextFillColorDisabledBrush}" />
                <Button Content="创建连接"
                        Style="{StaticResource AccentButtonStyle}"
                        HorizontalAlignment="Center"
                        Margin="0,8,0,0"
                        Click="NewConnection_Click" />
            </StackPanel>
        </Grid>
    </Grid>
</Page>
