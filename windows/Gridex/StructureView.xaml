<UserControl
    x:Class="Gridex.StructureView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Gridex">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <ScrollViewer Grid.Row="0" VerticalScrollBarVisibility="Auto">
            <StackPanel x:Name="StructureContainer" Spacing="0" />
        </ScrollViewer>

        <!-- Bottom toolbar: Add Column + Apply Changes -->
        <Grid Grid.Row="1" Padding="8,4"
              Background="{ThemeResource LayerFillColorDefaultBrush}"
              BorderBrush="{ThemeResource DividerStrokeColorDefaultBrush}"
              BorderThickness="0,1,0,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>

            <Button x:Name="AddColumnBtn" Grid.Column="0"
                    Padding="8,4" Background="Transparent" BorderThickness="0">
                <StackPanel Orientation="Horizontal" Spacing="4">
                    <FontIcon Glyph="&#xE710;" FontSize="10" />
                    <TextBlock Text="列" FontSize="11" />
                </StackPanel>
            </Button>

            <TextBlock x:Name="PendingAlterText" Grid.Column="2"
                       FontSize="11" VerticalAlignment="Center"
                       Foreground="Orange" Margin="0,0,8,0" />
                <Button x:Name="ApplyAlterBtn" Grid.Column="3"
                    Content="应用" Padding="10,4" FontSize="11"
                    Style="{StaticResource AccentButtonStyle}"
                    Visibility="Collapsed" />
        </Grid>
    </Grid>
</UserControl>
